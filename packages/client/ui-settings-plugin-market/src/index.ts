/** Host loader entry for the marketplace-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin market tab. */
export function apply(): void {}
